@model IEnumerable<Exercice_5_MVC.ViewModel.OrderViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                Détails
            </th>
            @* <th>
                @Html.DisplayNameFor(model => model.OrderDetails[1].OrderId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderDetails[1].UnitPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderDetails[1].Quantity)
            </th> *@
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.ActionLink("Details", "Details", new { id = item.Id })
            </td>
                @* @foreach (var detailsItem in item.OrderDetails) {
                <td>
                        @Html.DisplayFor(modelItem => detailsItem.ArticleId)
                </td>
                <td>
                        @Html.DisplayFor(modelItem => detailsItem.UnitPrice)
                </td>
                <td>
                        @Html.DisplayFor(modelItem => detailsItem.Quantity)
                </td>
                } *@
        </tr>
        }
    </tbody>
</table>
